apiVersion: v1
kind: Service
metadata:
  name: dbapp-service
  namespace: production
spec:
  selector:
    app: dbapp
  clusterIP: None
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
